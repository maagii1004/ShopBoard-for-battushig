export interface Product {
  id: string;
  name: string;
  tag: string;
  description: string;
  price: string;
  image: string;
};

export type Comment = {
  comment: string;
  text: string;
  date: string;
};

